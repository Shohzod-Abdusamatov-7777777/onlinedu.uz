import React from "react";
import Layout from "./Layout";

const Profile = () => {
    return (
        <Layout>
            <h1 className="display-3 top-80 text-center text-green">Hello from Profile page</h1>
        </Layout>
    );
};

export default Profile;
